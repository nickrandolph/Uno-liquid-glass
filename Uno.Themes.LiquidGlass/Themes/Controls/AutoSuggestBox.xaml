<!--
  LiquidGlass.Uno — AutoSuggestBox.xaml
  Glass search field. The inner TextBox template keeps the "QueryButton" part
  (the control assigns QueryIcon to it and wires the click), and the suggestions
  popup floats on the heavier glass surface material.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- Chromeless button hosting the query icon inside the field -->
  <Style x:Key="LiquidGlassQueryButtonStyle" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{ThemeResource LiquidGlassSecondaryLabelBrush}" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="MinHeight" Value="0" />
    <Setter Property="MinWidth" Value="0" />
    <Setter Property="Width" Value="36" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Grid x:Name="RootGrid" Background="{TemplateBinding Background}">
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal" />
                <VisualState x:Name="PointerOver">
                  <VisualState.Setters>
                    <Setter Target="ContentPresenter.Opacity" Value="0.7" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Pressed">
                  <VisualState.Setters>
                    <Setter Target="ContentPresenter.Opacity" Value="0.5" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Disabled">
                  <VisualState.Setters>
                    <Setter Target="RootGrid.Opacity" Value="0.45" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <ContentPresenter x:Name="ContentPresenter"
                              Content="{TemplateBinding Content}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              AutomationProperties.AccessibilityView="Raw" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- The glass field used inside AutoSuggestBox: LiquidGlassTextBoxStyle plus a QueryButton column -->
  <Style x:Key="LiquidGlassAutoSuggestBoxTextBoxStyle" TargetType="TextBox">
    <Setter Property="Background" Value="{ThemeResource LiquidGlassControlFillBrush}" />
    <Setter Property="BorderBrush" Value="{ThemeResource LiquidGlassStrokeBrush}" />
    <Setter Property="Foreground" Value="{ThemeResource LiquidGlassLabelBrush}" />
    <Setter Property="PlaceholderForeground" Value="{ThemeResource LiquidGlassSecondaryLabelBrush}" />
    <Setter Property="SelectionHighlightColor" Value="{ThemeResource LiquidGlassTintColor}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="10" />
    <Setter Property="MinHeight" Value="40" />
    <Setter Property="Padding" Value="14,9,4,10" />
    <Setter Property="FontSize" Value="15" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TextBox">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal" />
                <VisualState x:Name="PointerOver">
                  <VisualState.Setters>
                    <Setter Target="BorderElement.Background" Value="{ThemeResource LiquidGlassFillPointerOverBrush}" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Focused">
                  <VisualState.Setters>
                    <Setter Target="BorderElement.Background" Value="{ThemeResource LiquidGlassFillPointerOverBrush}" />
                    <Setter Target="FocusRim.BorderBrush" Value="{ThemeResource LiquidGlassTintBrush}" />
                    <Setter Target="FocusRim.Opacity" Value="1" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Disabled">
                  <VisualState.Setters>
                    <Setter Target="BorderElement.Opacity" Value="0.45" />
                    <Setter Target="ContentElement.Opacity" Value="0.45" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <ContentPresenter x:Name="HeaderContentPresenter"
                              Grid.Row="0"
                              Content="{TemplateBinding Header}"
                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                              Foreground="{ThemeResource LiquidGlassSecondaryLabelBrush}"
                              FontSize="13"
                              Margin="2,0,0,6"
                              Visibility="Collapsed" />

            <Grid Grid.Row="1" MinHeight="{TemplateBinding MinHeight}">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>

              <Border x:Name="BorderElement"
                      Grid.ColumnSpan="2"
                      Background="{TemplateBinding Background}"
                      CornerRadius="{TemplateBinding CornerRadius}" />
              <Border x:Name="Rim"
                      Grid.ColumnSpan="2"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      CornerRadius="{TemplateBinding CornerRadius}"
                      IsHitTestVisible="False" />
              <Border x:Name="FocusRim"
                      Grid.ColumnSpan="2"
                      BorderThickness="1.5"
                      CornerRadius="{TemplateBinding CornerRadius}"
                      Opacity="0"
                      IsHitTestVisible="False" />

              <ScrollViewer x:Name="ContentElement"
                            Padding="{TemplateBinding Padding}"
                            HorizontalScrollMode="Auto"
                            HorizontalScrollBarVisibility="Hidden"
                            VerticalScrollMode="Auto"
                            VerticalScrollBarVisibility="Hidden"
                            IsTabStop="False"
                            ZoomMode="Disabled"
                            AutomationProperties.AccessibilityView="Raw"
                            VerticalAlignment="Center" />
              <TextBlock x:Name="PlaceholderTextContentPresenter"
                         Text="{TemplateBinding PlaceholderText}"
                         Foreground="{TemplateBinding PlaceholderForeground}"
                         Padding="{TemplateBinding Padding}"
                         VerticalAlignment="Center"
                         IsHitTestVisible="False" />

              <!-- AutoSuggestBox assigns its QueryIcon as this button's content -->
              <Button x:Name="QueryButton"
                      Grid.Column="1"
                      Style="{StaticResource LiquidGlassQueryButtonStyle}"
                      Margin="0,2,4,2"
                      IsTabStop="False"
                      AutomationProperties.AccessibilityView="Raw" />
            </Grid>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="LiquidGlassAutoSuggestBoxStyle" TargetType="AutoSuggestBox">
    <Setter Property="FontSize" Value="15" />
    <Setter Property="TextBoxStyle" Value="{StaticResource LiquidGlassAutoSuggestBoxTextBoxStyle}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="AutoSuggestBox">
          <Grid x:Name="LayoutRoot">
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="Orientation">
                <VisualState x:Name="Landscape" />
                <VisualState x:Name="Portrait" />
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <TextBox x:Name="TextBox"
                     Style="{TemplateBinding TextBoxStyle}"
                     PlaceholderText="{TemplateBinding PlaceholderText}"
                     Header="{TemplateBinding Header}"
                     Description="{TemplateBinding Description}"
                     Width="{TemplateBinding Width}"
                     FontSize="{TemplateBinding FontSize}"
                     FontFamily="{TemplateBinding FontFamily}"
                     Canvas.ZIndex="0"
                     Margin="0"
                     DesiredCandidateWindowAlignment="BottomEdge"
                     UseSystemFocusVisuals="{TemplateBinding UseSystemFocusVisuals}" />

            <Popup x:Name="SuggestionsPopup">
              <Border x:Name="SuggestionsContainer"
                      Background="{ThemeResource LiquidGlassSurfaceBrush}"
                      BorderBrush="{ThemeResource LiquidGlassStrokeBrush}"
                      BorderThickness="1"
                      CornerRadius="16"
                      Padding="4"
                      Margin="0,4,0,0">
                <ListView x:Name="SuggestionsList"
                          Background="Transparent"
                          DisplayMemberPath="{TemplateBinding DisplayMemberPath}"
                          IsItemClickEnabled="True"
                          ItemTemplate="{TemplateBinding ItemTemplate}"
                          ItemTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                          ItemContainerStyle="{TemplateBinding ItemContainerStyle}"
                          MaxHeight="296"
                          Margin="0" />
              </Border>
            </Popup>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
