<!--
  LiquidGlass.Uno — TextBox.xaml
  Custom templates for text inputs: translucent glass field with rim light,
  tint-colored border when focused. (Uno's stock Fluent templates don't honor
  app-level lightweight resource overrides, so the theme retemplates these,
  the same approach Uno.Material takes.)
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Style x:Key="LiquidGlassTextBoxStyle" TargetType="TextBox">
    <Setter Property="Background" Value="{ThemeResource LiquidGlassControlFillBrush}" />
    <Setter Property="BorderBrush" Value="{ThemeResource LiquidGlassStrokeBrush}" />
    <Setter Property="Foreground" Value="{ThemeResource LiquidGlassLabelBrush}" />
    <Setter Property="PlaceholderForeground" Value="{ThemeResource LiquidGlassSecondaryLabelBrush}" />
    <Setter Property="SelectionHighlightColor" Value="{ThemeResource LiquidGlassTintColor}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="10" />
    <Setter Property="MinHeight" Value="40" />
    <Setter Property="Padding" Value="14,9,14,10" />
    <Setter Property="FontSize" Value="15" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TextBox">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal" />
                <VisualState x:Name="PointerOver">
                  <VisualState.Setters>
                    <Setter Target="BorderElement.Background" Value="{ThemeResource LiquidGlassFillPointerOverBrush}" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Focused">
                  <VisualState.Setters>
                    <Setter Target="BorderElement.Background" Value="{ThemeResource LiquidGlassFillPointerOverBrush}" />
                    <Setter Target="FocusRim.BorderBrush" Value="{ThemeResource LiquidGlassTintBrush}" />
                    <Setter Target="FocusRim.Opacity" Value="1" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Disabled">
                  <VisualState.Setters>
                    <Setter Target="BorderElement.Opacity" Value="0.45" />
                    <Setter Target="ContentElement.Opacity" Value="0.45" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <ContentPresenter x:Name="HeaderContentPresenter"
                              Grid.Row="0"
                              Content="{TemplateBinding Header}"
                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                              Foreground="{ThemeResource LiquidGlassSecondaryLabelBrush}"
                              FontSize="13"
                              Margin="2,0,0,6"
                              Visibility="Collapsed" />

            <Grid Grid.Row="1" MinHeight="{TemplateBinding MinHeight}">
              <Border x:Name="BorderElement"
                      Background="{TemplateBinding Background}"
                      CornerRadius="{TemplateBinding CornerRadius}" />
              <Border x:Name="Rim"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      CornerRadius="{TemplateBinding CornerRadius}"
                      IsHitTestVisible="False" />
              <Border x:Name="FocusRim"
                      BorderThickness="1.5"
                      CornerRadius="{TemplateBinding CornerRadius}"
                      Opacity="0"
                      IsHitTestVisible="False" />

              <ScrollViewer x:Name="ContentElement"
                            Padding="{TemplateBinding Padding}"
                            HorizontalScrollMode="Auto"
                            HorizontalScrollBarVisibility="Hidden"
                            VerticalScrollMode="Auto"
                            VerticalScrollBarVisibility="Hidden"
                            IsHorizontalRailEnabled="True"
                            IsVerticalRailEnabled="True"
                            IsTabStop="False"
                            ZoomMode="Disabled"
                            AutomationProperties.AccessibilityView="Raw"
                            VerticalAlignment="Center" />
              <TextBlock x:Name="PlaceholderTextContentPresenter"
                         Text="{TemplateBinding PlaceholderText}"
                         Foreground="{TemplateBinding PlaceholderForeground}"
                         Padding="{TemplateBinding Padding}"
                         VerticalAlignment="Center"
                         IsHitTestVisible="False" />
            </Grid>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="LiquidGlassPasswordBoxStyle" TargetType="PasswordBox">
    <Setter Property="Background" Value="{ThemeResource LiquidGlassControlFillBrush}" />
    <Setter Property="BorderBrush" Value="{ThemeResource LiquidGlassStrokeBrush}" />
    <Setter Property="Foreground" Value="{ThemeResource LiquidGlassLabelBrush}" />
    <Setter Property="SelectionHighlightColor" Value="{ThemeResource LiquidGlassTintColor}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="10" />
    <Setter Property="MinHeight" Value="40" />
    <Setter Property="Padding" Value="14,9,14,10" />
    <Setter Property="FontSize" Value="15" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="PasswordBox">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal" />
                <VisualState x:Name="PointerOver">
                  <VisualState.Setters>
                    <Setter Target="BorderElement.Background" Value="{ThemeResource LiquidGlassFillPointerOverBrush}" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Focused">
                  <VisualState.Setters>
                    <Setter Target="BorderElement.Background" Value="{ThemeResource LiquidGlassFillPointerOverBrush}" />
                    <Setter Target="FocusRim.BorderBrush" Value="{ThemeResource LiquidGlassTintBrush}" />
                    <Setter Target="FocusRim.Opacity" Value="1" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Disabled">
                  <VisualState.Setters>
                    <Setter Target="BorderElement.Opacity" Value="0.45" />
                    <Setter Target="ContentElement.Opacity" Value="0.45" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <ContentPresenter x:Name="HeaderContentPresenter"
                              Grid.Row="0"
                              Content="{TemplateBinding Header}"
                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                              Foreground="{ThemeResource LiquidGlassSecondaryLabelBrush}"
                              FontSize="13"
                              Margin="2,0,0,6"
                              Visibility="Collapsed" />

            <Grid Grid.Row="1" MinHeight="{TemplateBinding MinHeight}">
              <Border x:Name="BorderElement"
                      Background="{TemplateBinding Background}"
                      CornerRadius="{TemplateBinding CornerRadius}" />
              <Border x:Name="Rim"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      CornerRadius="{TemplateBinding CornerRadius}"
                      IsHitTestVisible="False" />
              <Border x:Name="FocusRim"
                      BorderThickness="1.5"
                      CornerRadius="{TemplateBinding CornerRadius}"
                      Opacity="0"
                      IsHitTestVisible="False" />

              <ScrollViewer x:Name="ContentElement"
                            Padding="{TemplateBinding Padding}"
                            HorizontalScrollMode="Auto"
                            HorizontalScrollBarVisibility="Hidden"
                            VerticalScrollMode="Auto"
                            VerticalScrollBarVisibility="Hidden"
                            IsTabStop="False"
                            ZoomMode="Disabled"
                            AutomationProperties.AccessibilityView="Raw"
                            VerticalAlignment="Center" />
              <TextBlock x:Name="PlaceholderTextContentPresenter"
                         Text="{TemplateBinding PlaceholderText}"
                         Foreground="{ThemeResource LiquidGlassSecondaryLabelBrush}"
                         Padding="{TemplateBinding Padding}"
                         VerticalAlignment="Center"
                         IsHitTestVisible="False" />
            </Grid>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
