<!--
  LiquidGlass.Uno — Menus.xaml
  Menu and overlay surfaces: flyouts, menu flyouts, and dialogs float on the heavier
  glass material. Menu rows highlight with the tint on hover, like macOS menus.

  FlyoutPresenter / MenuFlyoutPresenter / ContentDialog keep their stock templates —
  those templates bind Background/BorderBrush/CornerRadius, so property styles are
  enough. MenuFlyoutItem and MenuFlyoutSeparator are retemplated.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Style x:Key="LiquidGlassFlyoutPresenterStyle" TargetType="FlyoutPresenter">
    <Setter Property="Background" Value="{ThemeResource LiquidGlassSurfaceBrush}" />
    <Setter Property="BorderBrush" Value="{ThemeResource LiquidGlassStrokeBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="16" />
    <Setter Property="Padding" Value="16" />
    <Setter Property="Foreground" Value="{ThemeResource LiquidGlassLabelBrush}" />
  </Style>

  <Style x:Key="LiquidGlassMenuFlyoutPresenterStyle" TargetType="MenuFlyoutPresenter">
    <Setter Property="Background" Value="{ThemeResource LiquidGlassSurfaceBrush}" />
    <Setter Property="BorderBrush" Value="{ThemeResource LiquidGlassStrokeBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="16" />
    <Setter Property="Padding" Value="4" />
  </Style>

  <!-- macOS-style menu row: tint highlight, white text on hover -->
  <Style x:Key="LiquidGlassMenuFlyoutItemStyle" TargetType="MenuFlyoutItem">
    <Setter Property="Foreground" Value="{ThemeResource LiquidGlassLabelBrush}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Padding" Value="12,8" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="MinWidth" Value="160" />
    <Setter Property="UseSystemFocusVisuals" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="MenuFlyoutItem">
          <Grid x:Name="LayoutRoot"
                Background="{TemplateBinding Background}"
                CornerRadius="12"
                Padding="{TemplateBinding Padding}">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal" />
                <VisualState x:Name="PointerOver">
                  <VisualState.Setters>
                    <Setter Target="LayoutRoot.Background" Value="{ThemeResource LiquidGlassTintBrush}" />
                    <Setter Target="TextBlock.Foreground" Value="{ThemeResource LiquidGlassOnTintBrush}" />
                    <Setter Target="KeyboardAcceleratorTextBlock.Foreground" Value="{ThemeResource LiquidGlassOnTintBrush}" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Pressed">
                  <VisualState.Setters>
                    <Setter Target="LayoutRoot.Background" Value="{ThemeResource LiquidGlassTintPressedBrush}" />
                    <Setter Target="TextBlock.Foreground" Value="{ThemeResource LiquidGlassOnTintBrush}" />
                    <Setter Target="KeyboardAcceleratorTextBlock.Foreground" Value="{ThemeResource LiquidGlassOnTintBrush}" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Disabled">
                  <VisualState.Setters>
                    <Setter Target="LayoutRoot.Opacity" Value="0.45" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <TextBlock x:Name="TextBlock"
                       Text="{TemplateBinding Text}"
                       Foreground="{TemplateBinding Foreground}"
                       FontSize="{TemplateBinding FontSize}"
                       VerticalAlignment="Center" />
            <TextBlock x:Name="KeyboardAcceleratorTextBlock"
                       Grid.Column="1"
                       Foreground="{ThemeResource LiquidGlassSecondaryLabelBrush}"
                       FontSize="12"
                       Margin="24,0,0,0"
                       VerticalAlignment="Center"
                       Visibility="Collapsed" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="LiquidGlassMenuFlyoutSeparatorStyle" TargetType="MenuFlyoutSeparator">
    <Setter Property="Background" Value="{ThemeResource LiquidGlassSeparatorBrush}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="MenuFlyoutSeparator">
          <Border Background="{TemplateBinding Background}" Height="1" Margin="12,4" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Dialogs float on the heavier surface; command buttons use the glass capsules -->
  <Style x:Key="LiquidGlassContentDialogStyle" TargetType="ContentDialog">
    <Setter Property="Background" Value="{ThemeResource LiquidGlassSurfaceBrush}" />
    <Setter Property="BorderBrush" Value="{ThemeResource LiquidGlassStrokeBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="26" />
    <Setter Property="Foreground" Value="{ThemeResource LiquidGlassLabelBrush}" />
    <Setter Property="PrimaryButtonStyle" Value="{StaticResource LiquidGlassDialogProminentButtonStyle}" />
    <Setter Property="SecondaryButtonStyle" Value="{StaticResource LiquidGlassDialogButtonStyle}" />
    <Setter Property="CloseButtonStyle" Value="{StaticResource LiquidGlassDialogButtonStyle}" />
  </Style>
</ResourceDictionary>
